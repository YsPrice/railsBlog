<div id="<%= dom_id post %>">
  <p>
  <h4> post belongs to 
  <%= post.user.name%>
  </h4>
    <strong>Title:</strong>

    <%= post.title %>
    
    <h6><%=pluralize(post.views, "view")%></h6>
  </p>

  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>

</div>
