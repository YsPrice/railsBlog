<% if current_user %>
<li class="nav-item dropdown">
<a class="nav-link dropdow-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
<span class="badge rounded-pill bg-danger">
Notifications:
<%= @unread.count >= 9 ? "9+" : @unread.count%> 
</span>
</a>
<ul class="dropdown-menu dropdown-menu-lg-end" style="width:180%; padding:11%; margin:17%;" aria-labelledby="navbarDropdown">
<% @unread.each do |notification|%>
<%= render 'layouts/notification', notification: notification %>
<% end %>
<%if @read.count > 0 && @unread.count > 0%>
<li>
<hr class="dropdown-divider">
</li>
<% elsif @read.count + @unread.count == 0%>
<li class="dropdown-item">
no notifications yet.
</li>
<% end %>
</ul>
</li>
<% end %>